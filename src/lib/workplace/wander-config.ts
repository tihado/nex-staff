/** Chance idle staff actually walks on each wander tick (otherwise stays put). */
export const STAFF_WANDER_MOVE_CHANCE = 0.7;

/** Idle staff in pantry: chance per wander tick to visit the work area. */
export const STAFF_IDLE_WORKPLACE_VISIT_CHANCE = 0.12;

/** While visiting the work area, chance per wander tick to return to pantry. */
export const STAFF_IDLE_PANTRY_RETURN_CHANCE = 0.45;
